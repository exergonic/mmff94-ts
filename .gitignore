dist/
node_modules/
*.tsbuildinfo

# Local development
.vite/

# Build and log files (but not tracked reference + suite logs)
*.log
!tests/references/*.log
!tests/references/tinker/*.log
!tests/fixtures/validation-suite/*.log
!tests/references/*.log
!tests/references/tinker/*.log

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp
*.swo

# Local project guide — not for GitHub
AGENTS.md

# OpenBabel sources — reference only, not part of the library
temp_ob/
data/

# Local opencode configuration
.opencode/

# OpenBabel Python virtual environment for reference generation
tests/scripts/.venv/

# temporary ob runs (legacy)
ob_runs
temp_tinker/
temp_tinker/
__pycache__/
*.pyc
*.tgz
